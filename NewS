package com.company;

import java.io.IOException;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashMap;
import java.util.Map;

public class Main {


    public static void main(String[] args)throws IOException {
       Integer[][] buildings = {{1,11,5},{2,6,7},{3,13,9},{12,7,16},{14,3,25},{19,18,22},{23,13,29},{24,4,28}};
       ArrayList<Integer[]> b = new ArrayList<>() ;
       Map map = new HashMap();
       Map result = new HashMap();

       for(int i = 0; i < buildings.length; i++){
            b.add(buildings[i]);
           System.out.println(b.get(i)[0]);
           map.put(b.get(i)[0], -b.get(i)[1]);
           map.put(b.get(i)[2], b.get(i)[1]);
       }

       result.put(0,true);
       Integer maxV = 0;
       for(int i = 0; i < map.size(); i++){

       }
        System.out.println(map);
    }
    public static Integer bufferM(ArrayList<Integer> num){
        return Collections.max(num);
    }
}

https://www.youtube.com/watch?v=mrqLYF362GU
